export enum StockMovementEnum {
  BUY = 'BUY', // entrada
  SELL = 'SELL', // saida
  ADJUST_IN = 'ADJUST_IN', // entrada
  ADJUST_OUT = 'ADJUST_OUT', // saida
  RETURN = 'RETURN', // entrada
  PRODUCTION = 'PRODUCTION', // entrada
}
